[pytest]
# The sampler-film tests rely on hardcoded relative paths and perf monitors, so
# the default suite targets the cleanly-collectable tests only. Run others by
# passing their path explicitly.
testpaths =
    tests
    experimental/experiments/test_cosmic_collapse.py
    experimental/tools/test_contact_sheet.py
