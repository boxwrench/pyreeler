# v0.1 Release Readiness Agent Batch

Implemented on 2026-07-12 after the dependable-renderer milestone.

## Result

- Public package metadata now includes the README, SPDX license, author,
  supported Python versions, project URLs, and video topic classification.
- `CHANGELOG.md` records the v0.1.0 boundary without claiming an unverified
  package-index publication.
- CI covers Ubuntu/Python 3.10, Ubuntu/Python 3.12, and Windows/Python 3.12.
  The clean-wheel FFmpeg fallback smoke remains a single Ubuntu/Python 3.12 job.
- Four copied experimental references were removed. Experimental documentation
  now links to shared or explicitly provider-specific canonical references.

## Validation

- 132 tests passed.
- Provider synchronization and template graduation checks passed.
- A clean wheel built without metadata deprecation warnings.
- Installed-wheel renders passed with system FFmpeg and imageio-ffmpeg fallback.

## Final Release Gate

Passed with an installed-wheel TUI render to `lorenz-3.mp4`. The threaded Render
path completed, no-clobber naming selected a new output, and Play dispatched the
finished file. FFprobe reported H.264, 320x180, yuv420p, 12 fps, 96 decoded
frames, and exactly eight seconds. A six-frame contact sheet showed a readable
progression from sparse trace to the complete rotating two-lobed attractor.

The repository is ready to commit and create the annotated `v0.1.0` tag.